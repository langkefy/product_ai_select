<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.select.product.mapper.RankingHistoryMapper">

    <select id="selectByProductAndType" resultType="com.select.product.entity.RankingHistory">
        SELECT * FROM ranking_history
        WHERE product_id = #{productId}
          AND rank_type = #{rankType}
          AND stat_date >= DATE_SUB(CURDATE(), INTERVAL #{days} DAY)
        ORDER BY stat_date ASC
    </select>

    <select id="selectLatestRank" resultType="java.lang.Integer">
        SELECT rank_position FROM ranking_history
        WHERE product_id = #{productId}
          AND rank_type = #{rankType}
          AND stat_date = #{statDate}
        LIMIT 1
    </select>

</mapper>

